apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: newscaster-api:latest
        imagePullPolicy: Never
        env:
        - name: BUCKET_NAME
          valueFrom:
            secretKeyRef:
              name: gcs-bucket
              key: BUCKET_NAME
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-app
              key: DATABASE_URL
        ports:
        - containerPort: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector:
    app: api
  ports:
  - port: 8000
    targetPort: 8000
